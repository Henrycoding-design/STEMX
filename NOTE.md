This main branch feature implement a stateless architecture:
+ files are uploaded as base64 inline
+ use generateContent API
+ files are uploaded every request
+ no stateful management

-> Simple, lightweight, faster and more predictable